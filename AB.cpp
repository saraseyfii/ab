#include<iostream>
using namespace std;

	int a, b;
	cin >> a >> b;
	if (a >= 0 && b >= o) {
		cout << "posetive";
	}
	if (a < 0 && b < 0) {
		cout << "negative";
	}
	if (a > b) {
		cout << "a>b";
	}
	if (a > 0) {
		cout << "a+";
	}
	else {
		cout << "a-";
	} if (b > 0) {
		cout << "b+";
	}
	else {
		cout"b-";
		return 0;
	}